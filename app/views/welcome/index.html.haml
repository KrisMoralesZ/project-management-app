%div.jumbotron
  %p.lead You'll love managing your project with our SaaS application. Please take a look at our great plans

%div.w-75.d-flex.jusitfy-content-center.align-items-center.justify-content-around.mx-auto.mt-5
  %div.card.card-width
    %div.card-body
      %p.card-title SignUp for Free
      %ul
        %li.card-text Unlimited file uploads
        %li.card-text Access anywhere
      = link_to 'Sign up now!', new_organization_registration_path, class: 'btn btn-primary'

  %div.card.card-width
    %div.card-body
      %p.card-title Do you have an Organization?
      = form_with url: go_to_organization_path, method: :post, local:true, data: { turbo: false } do |f|
        .field
          = f.label :organization_name
          %br/
          = f.text_field :organization_name, autofocus: true, autocomplete: "organization_name"
        .actions.mt-2
          = f.submit "Login as Administrator", name: "commit", value: "Admin", class: "btn btn-primary"
          = f.submit "Login as Collaborator", name: "commit", value: "Member", class: "btn btn-secondary"
